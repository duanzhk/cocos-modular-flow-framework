{
    "extends": "./base.tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",// 只编译 src 目录
        "emitDecoratorMetadata": true, // 启用元数据反射
        "baseUrl": "./static/assets", // paths的起始目录
        "paths": {
            "@core": ["core/index"],
            "@libs": ["libs/index"],
            "@utils/*": ["utils/*"]
        }
    },
    "exclude": [
        "static/assets/**/*" // 排除 static/assets 目录
    ],
}